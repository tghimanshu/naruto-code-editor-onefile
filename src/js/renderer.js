/**
 * @fileoverview Main renderer script that attaches event listeners to UI elements.
 * Links the "Open File" and "Save File" buttons to their respective functions.
 */

openFileBtn.addEventListener("click", openFile);

saveFileBtn.addEventListener("click", saveFile);
